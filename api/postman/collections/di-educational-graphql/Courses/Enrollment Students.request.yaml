$kind: graphql-request
name: Enrollment Students
url: '{{baseUrl}}/graphql'
order: 7000
query: |-
  mutation EnrollmentStudents($courseId: Int!, $ids: [Int!]!) {
    enrollmentStudents(courseId: $courseId, ids: $ids)
  }
variables: '{"courseId": 1, "ids": [1, 2]}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test('Status code is 200', function () {
        pm.response.to.have.status(200);
      });

      pm.test('Response has no GraphQL errors', function () {
        const json = pm.response.json();
        pm.expect(json).to.not.have.property('errors');
      });

      pm.test('data.enrollmentStudents is a boolean', function () {
        const json = pm.response.json();
        pm.expect(json.data.enrollmentStudents).to.be.a('boolean');
      });
